# FlaskUserAPI
API REST per la gestione utenti con autenticazione. Implementazione endpoint CRUD (Create, Read, Update, Delete), Gestione dati in formato JSON, Password hashing per sicurezza, Strutturazione backend con architettura logica.
